import React from "react";
import styled from "styled-components";

const FiltersContainer = styled.div`
  margin: 16px 0px;
  }
`;

function Filters(props) {
    return (
      <FiltersContainer>
       Select filters go here:

       Location
      </FiltersContainer>
    );
  }
  
  export default Filters;